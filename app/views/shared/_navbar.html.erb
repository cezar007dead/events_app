<nav class="navbar navbar-expand-lg navbar-dark bg-warning">
  <div class="container-fluid">
    <%= link_to "Events HOME", root_path, class: "navbar-brand text-dark" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if user_signed_in? %>
      	<li class="nav-item mx-2">
	        <%= link_to "All events", events_path, class: "btn btn-primary" %>
	      </li>
        <li class="nav-item mx-2">
	        <%= link_to "Create Event", new_event_path, class: "btn btn-success" %>
	      </li>
      <% end %>
      </ul>
      <% if user_signed_in? %>
      	<li class="nav-item mx-2">
	        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
	      </li>
      <% else %>
        <div class="nav-item mx-2">
          <%= link_to "Login", new_user_session_path, class: "btn btn-danger" %>
        </div> 
        <div class="nav-item mx-2">
          <%= link_to "Signup", new_user_registration_path, class: "btn btn-danger" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>